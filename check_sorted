def is_sorted(arr):

    for i in range(1, len(arr)):

        if arr[i] < arr[i-1]:
            return False
        return True
    


arr = input("Enter the array : ").split()

arr = [ int(x) for x in arr]

print("Is array of number sorted", is_sorted(arr))